LIB_DIR = lib

default: isisimtq

pyexamples: setup.py isisimtq.pyx $(LIB_DIR)/libants.a
	python3 setup.py build_ext --inplace && rm -f isisants.c && rm -Rf build

$(LIB_DIR)/libants.a:
	make -C $(LIB_DIR) libants.a

clean:
	rm *.so
